# Greentic Java Fixture

Small Swing fixture for Java accessibility automation tests.

```bash
./build.sh
java -cp build CustomerForm
```

The window title is `Greentic Java Fixture`. It exposes accessible controls named
`customer_name`, `email`, `Save`, and `confirmation_id`.
